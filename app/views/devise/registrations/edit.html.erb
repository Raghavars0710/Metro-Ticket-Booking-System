<!DOCTYPE html>
<html>
<head>
  <title>Edit <%= resource_name.to_s.humanize %></title>
  <style>

    body {
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      font-family: Arial, sans-serif;
    }

    .form-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .field {
      margin-bottom: 10px;
    }

    .large-input {
      width: 300px;
      padding: 10px;
      font-size: 16px;
    }

    .actions {
      text-align: center;
    }

    .cancel-account-button {
      background-color: #f44336;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
      text-decoration: none;
    }

    .cancel-account-button:hover {
      background-color: #d32f2f;
    }

    .back-link {
      display: block;
      margin-top: 20px;
      text-align: center;
      color: #008CBA;
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }

    h2 {
      font-size: 24px;
      margin-bottom: 20px;
      color: #007bff;
    }

    h3 {
/*      margin-top: 40px;*/
      font-size: 20px;
    }

    .cancel-account-button {
      margin-top: 10px;
    }

    .form-container {
      margin-bottom: 30px;
    }
  </style>
</head>
<body>

   <div class="form-container">
    <h2>Edit <%= resource_name.to_s.humanize %> Profile</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <label for="user_name">Name:</label>
        <%= f.text_field :name, autocomplete: "name" %>
      </div>

      <div class="field">
        <label for="user_contact_number">Phone Number:</label>
        <%= f.text_field :contact_number, placeholder: "Contact Number" %>
      </div>

      <div class="field">
        <label for="user_address">Address:</label>
        <%= f.text_field :address, placeholder: "Address" %>
      </div>

      <div class="field">
        <label for="user_gender">Gender:</label>
        <%= f.select :gender, options_for_select(['male', 'female', 'other']), value: current_user.gender %>
      </div>

      <div class="field">
        <label for="user_date_of_birth">DOB:</label>
        <%= f.text_field :date_of_birth, placeholder: "Date Of Birth" %>
      </div>

      <div class="field">
        <label for="user_current_password">Current Password:</label>
        <%= f.password_field :current_password, autocomplete: "current-password" %>
      </div>

      <div class="actions">
        <%= f.submit "Update" %>
      </div>
    <% end %>

    <br><br>
    <h3>Cancel my account</h3>

    <div>
      <%= button_to "Delete Account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: "cancel-account-button" %>
    </div>
  </div>

</body>
</html>

